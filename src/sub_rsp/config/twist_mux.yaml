twist_mux:
  ros__parameters:
    use_stamped: true
    topics:
      joystick:
        topic   : cmd_vel_joy
        timeout : 0.5
        priority: 90
    locks:
      e_stop:
        topic   : e_stop
        timeout : 0.0
        priority: 255